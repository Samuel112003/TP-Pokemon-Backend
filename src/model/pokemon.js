const Pokemon = {
    id: Number,
    name: String,
    types: [String],
    stats: {
        hp: Number,
        attack: Number,
        defense: Number,
        spAttack: Number,
        spDefense: Number,
        speed: Number
    }
}

module.exports = {
    Pokemon
}